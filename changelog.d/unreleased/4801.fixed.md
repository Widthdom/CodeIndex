---
category: fixed
issues:
  - 4801
affected:
  - tests/CodeIndex.Tests/ProgramRunnerTests.cs
  - TESTING_GUIDE.md
---

## English

- **Installer cancellation coverage now waits for a complete PID under load (#4801)** — the hung-installer test uses a bounded readiness waiter and starts cancellation only after the PID file contains a positive integer, avoiding empty-file parse failures during parallel test runs.

## 日本語

- **installer cancellation のテストが高負荷時にも完全な PID を待つようになりました (#4801)** — hung installer のテストは上限付き readiness waiter を使い、PID ファイルが正の整数を含んだ後にだけ cancellation を開始するため、並列テスト実行時の空ファイルによる parse failure を防ぎます。
