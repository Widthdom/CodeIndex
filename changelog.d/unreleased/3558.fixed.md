---
category: fixed
issues:
  - 3558
affected:
  - DEVELOPER_GUIDE.md
  - src/CodeIndex/Cli/QueryCommandRunner.cs
  - src/CodeIndex/Cli/SearchSnippetFormatter.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerSearchHintTests.cs
---

## English

- **Search JSON now exposes effective snippet options and raw-FTS highlight availability (#3558)** — compact CLI rows include `snippet_lines`, `max_line_width`, `exact`, `raw_fts`, `literal_highlights_available`, and `literal_highlight_warning` so clients can distinguish literal highlight support from raw FTS syntax gaps.

## 日本語

- **検索 JSON が有効な snippet options と raw FTS の highlight 可否を返すようになりました (#3558)** — compact CLI row に `snippet_lines` / `max_line_width` / `exact` / `raw_fts` / `literal_highlights_available` / `literal_highlight_warning` を追加し、literal highlight 対応と raw FTS 構文由来の不足を区別できるようにしました。
