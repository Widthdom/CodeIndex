---
category: internal
issues:
  - 4966
affected:
  - tests/CodeIndex.Tests/IndexWatchRunnerTests.cs
  - TESTING_GUIDE.md
---

## English

- **Made ancestor-ignore watcher reconciliation coverage deterministic on .NET 9 (#4966)** — The subproject watcher test now injects the production enqueue callback after writing the ancestor `.gitignore`, separating reconciliation coverage from unreliable macOS `FileSystemWatcher` delivery while retaining the bounded end-to-end database assertion.

## 日本語

- **.NET 9 で祖先 ignore watcher の reconciliation coverage を決定的にしました (#4966)** — subproject watcher test は祖先 `.gitignore` の書き込み後に本番 enqueue callback を注入するようになり、上限付きの end-to-end database assertion を維持しながら、reconciliation coverage を不安定な macOS `FileSystemWatcher` 配信から分離しました。
