---
category: fixed
issues:
  - 4622
affected:
  - LSP references
  - LSP symbol locations
  - LSP inlay hints
---

## English

- Made LSP references use the CLI's indexed candidate identity, returned identifier ranges for document and workspace symbols, and suppressed type hints for explicit method, local, and field types.

## 日本語

- LSP reference が CLI と同じ indexed candidate identity を使うようにし、document/workspace symbol で identifier range を返し、method、local、field の明示型に対する type hint を抑制しました。
