---
category: fixed
issues:
  - 4689
affected:
  - tests/CodeIndex.HookIsolationFixture/
  - tests/CodeIndex.Tests/PostExtractionHookTests.cs
  - TESTING_GUIDE.md
---

## English

- **Post-extraction hook isolation coverage no longer fans out across every test hook (#4689)** — the duplicate-hook timeout test now copies a dedicated minimal fixture assembly, keeping the healthy callback worker deterministic in the full `net8.0` suite while preserving module-initializer isolation and per-hook-ID disablement coverage.

## 日本語

- **post-extraction hook の isolation coverage が全 test hook へ fan-out しないようになりました (#4689)** — duplicate-hook timeout test は専用の最小 fixture assembly を copy するようになり、module initializer の隔離と hook ID 単位の disablement coverage を維持したまま、`net8.0` full suite でも正常な callback worker を deterministic に検証できます。
