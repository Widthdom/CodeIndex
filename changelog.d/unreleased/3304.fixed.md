---
category: fixed
issues:
  - 3304
affected:
  - src/CodeIndex/Mcp/AuditLogSink.cs
  - tests/CodeIndex.Tests/McpAuditLogTests.cs
---

## English

- **MCP audit request-id truncation coverage is reachable again (#3304)** — audit log request IDs now use a shorter display bound than the JSON-RPC request-id accept limit, so valid tool calls can still exercise and emit `request_id_truncated` metadata.

## 日本語

- **MCP audit の request-id 短縮カバレッジが再び到達可能になりました (#3304)** — audit log の request ID 表示上限を JSON-RPC request-id 受理上限より短くし、有効な tool call でも `request_id_truncated` metadata を検証・出力できるようにしました。
