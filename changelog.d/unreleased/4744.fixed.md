---
category: fixed
issues:
  - 4744
affected:
  - src/CodeIndex/Cli/QueryCommandRunner.Definition.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerSymbolTests.cs
  - DEVELOPER_GUIDE.md
---

## English

- **Missing definitions now return structured JSON errors (#4744)** — default-format `definition --json` lookups emit the versioned `E018_QUERY_NOT_FOUND` error and exit `2` when no symbol matches, with or without `--body`, instead of succeeding with empty stdout.

## 日本語

- **definition の未検出時に構造化された JSON error を返すようになりました (#4744)** — 既定 format の `definition --json` は、一致する symbol がない場合に `--body` の有無にかかわらず、空の stdout のまま成功せず、versioned `E018_QUERY_NOT_FOUND` error と終了コード `2` を返します。
