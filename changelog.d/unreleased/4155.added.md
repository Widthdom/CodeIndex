---
category: added
issues:
  - 4155
affected:
  - src/CodeIndex/Cli/SearchAuditRecipes.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerSearchTests.cs
---

## English

- **Resource materialization audits now classify subsystem hotspots (#4155)** - the new `resource-materialization-audit` recipe groups disposable boundaries, async disposal, DB command ownership, file-open sharing policy, stream materialization, and query/MCP `ToArray()` conversions into focused audit queries.

## 日本語

- **resource materialization audit が subsystem hotspot を分類するようになりました (#4155)** - 新しい `resource-materialization-audit` recipe は、disposable boundary、async disposal、DB command ownership、file-open sharing policy、stream materialization、query/MCP の `ToArray()` conversion を focused audit query として分類します。
