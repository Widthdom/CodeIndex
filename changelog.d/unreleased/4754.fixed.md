---
category: fixed
issues:
  - 4754
affected:
  - src/CodeIndex/Cli/QueryCommandRunner.Discovery.cs
  - src/CodeIndex/Cli/QueryCommandRunner.Map.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerFilesTests.cs
  - USER_GUIDE.md
  - TESTING_GUIDE.md
---

## English

- **`map --exclude-tests` now matches the `files` production-source scope (#4754)** — without an explicit `--path`, both commands apply the same production-source preset; explicit path scopes also keep matching cross-command file counts.

## 日本語

- **`map --exclude-tests` が `files` の本番ソース scope と一致するようになりました (#4754)** — 明示的な `--path` がない場合は両コマンドが同じ本番ソース preset を適用し、明示した path scope でもコマンド間の file count が一致します。
