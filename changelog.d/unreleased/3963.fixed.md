---
category: fixed
issues:
  - 3963
affected:
  - src/CodeIndex/Mcp/AuditLogSink.cs
  - src/CodeIndex/Mcp/McpServer.cs
  - tests/CodeIndex.Tests/AuditLogSinkTests.cs
  - tests/CodeIndex.Tests/McpServerTests.cs
---

## English

- **Runtime timing paths now use injectable time or wait handles (#3963)** — MCP health, keep-alive, audit timestamps, and request-cancellation tombstones now use the server `TimeProvider`, while audit-log idle waits use a completion signal instead of wall-clock polling and `Thread.Sleep`.

## 日本語

- **runtime timing path が injectable time または wait handle を使うようになりました (#3963)** — MCP health、keep-alive、audit timestamp、request-cancellation tombstone は server の `TimeProvider` を使い、audit-log idle wait は wall-clock polling と `Thread.Sleep` ではなく completion signal を使うようになりました。
