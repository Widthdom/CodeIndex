---
category: fixed
issues:
  - 4856
affected:
  - src/CodeIndex/Cli
  - src/CodeIndex/Database/DbContext.cs
  - tests/CodeIndex.Tests
  - USER_GUIDE.md
  - DEVELOPER_GUIDE.md
  - TESTING_GUIDE.md
---

## English

- **Database maintenance failures now share one versioned classifier and error contract (#4856)** — `vacuum`, `backfill-fold`, `optimize`, and `db integrity` classify SQLite result codes consistently, redact absolute paths unless `--show-paths` is explicit, keep JSON valid, and provide failure-specific recovery guidance. This also preserves the recovery intent of #1526 and #4338 without treating corruption as lock contention.

## 日本語

- **database maintenance の失敗を単一の versioned classifier と error contract に統一しました (#4856)** — `vacuum`、`backfill-fold`、`optimize`、`db integrity` は SQLite result code を一貫して分類し、`--show-paths` を明示しない限り absolute path を redaction し、有効な JSON と failure 別の recovery guidance を返します。corruption を lock contention と誤分類せず、#1526 と #4338 の recovery intent も維持します。
