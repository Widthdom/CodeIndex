---
category: fixed
issues:
  - 4567
affected:
  - src/CodeIndex/Cli/ExcerptRecoveryCommandFormatter.cs
  - src/CodeIndex/Cli/ProgramRunner.cs
  - src/CodeIndex/Models/QueryResults.cs
---

## English

- **Recovery hints now provide safe structured argv (#4567)** — excerpt and body recovery payloads preserve the current apphost or `dotnet` plus assembly prefix in a primary `argv` array, while the legacy display-only command is escaped for its declared POSIX sh or PowerShell context.

## 日本語

- **復旧ヒントが安全な構造化 argv を返すようになりました (#4567)** — excerpt と body の復旧 payload は、現在の apphost、または `dotnet` と assembly の prefix を一次契約の `argv` 配列に維持し、従来の表示専用 command は宣言した POSIX sh / PowerShell 向けに escape します。
