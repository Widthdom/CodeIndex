---
category: fixed
issues:
  - 4755
affected:
  - src/CodeIndex/Cli/CliFlagSchema.cs
  - tests/CodeIndex.Tests/ConsoleUiTests.cs
  - tests/CodeIndex.Tests/ProgramRunnerTests.cs
---

## English

- **`recipes --query` now accepts separated values (#4755)** — recipe listing now recognizes both `--query <filter>` and `--query=<filter>` through the canonical CLI flag schema, including option-shaped filter values while preserving a precise missing-value diagnostic.

## 日本語

- **`recipes --query` が分離形式の値を受け付けるようになりました (#4755)** — recipe 一覧は canonical CLI flag schema を通じて `--query <filter>` と `--query=<filter>` の両方を認識し、option 形式に見える filter 値も受け付けつつ、値がない場合の明確な診断を維持します。
