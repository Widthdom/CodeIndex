---
category: fixed
issues:
  - 4753
affected:
  - src/CodeIndex/Database/DbSymbolReader.Search.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerSymbolTests.cs
  - USER_GUIDE.md
---

## English

- **Hotspot-sorted symbols no longer repeat physical definitions (#4753)** — audit ranking now combines case-insensitive reference-name variants before ordering and limiting, so each `symbol_id` appears once and offset-based pages remain deterministic without overlap.

## 日本語

- **hotspot 順の symbols が同じ物理定義を重複して返さなくなりました (#4753)** — audit ranking は大文字小文字だけが異なる参照名を ordering / limit 適用前に統合するため、各 `symbol_id` は1回だけ返り、offset ベースのページも重複せず決定的な順序を維持します。
