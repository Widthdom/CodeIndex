---
category: fixed
issues:
  - 4442
affected:
  - USER_GUIDE.md
  - src/CodeIndex/Indexer/Scanning/FileIndexer.DefaultExclusions.cs
  - tests/CodeIndex.Tests/FileIndexerTests.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerFilesTests.cs
---

## English

- **Suggestion sidecars no longer make a fresh workspace stale (#4442)** — workspace scans now exclude the internal `.cdidx` data directory consistently, so creating `.cdidx/suggestions-codeindex.json` does not produce an `unindexed_workspace_files` status failure.

## 日本語

- **suggestion sidecar によって fresh な workspace が stale にならなくなりました (#4442)** — workspace scan が内部データディレクトリ `.cdidx` を一貫して除外するようになり、`.cdidx/suggestions-codeindex.json` の作成によって status が `unindexed_workspace_files` で失敗しなくなりました。
