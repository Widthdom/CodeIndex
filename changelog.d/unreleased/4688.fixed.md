---
category: fixed
issues:
  - 4688
affected:
  - src/CodeIndex/Cli/CliFlagSchema.cs
  - src/CodeIndex/Cli/QueryCommandRunner.Discovery.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerFilesTests.cs
---

## English

- **`symbols` and `files` now honor `--results-only` (#4688)** — their NDJSON row streams can suppress the terminal record as documented, while array, compact, summary, and count output reject the row-stream-only option explicitly.

## 日本語

- **`symbols` と `files` が `--results-only` に対応しました (#4688)** — ドキュメントどおり NDJSON row stream の終端レコードを省略できるようになり、array / compact / summary / count 出力では row stream 専用 option を明示的に拒否します。
