---
category: fixed
issues:
  - 4709
affected:
  - src/CodeIndex/Indexer/Symbols/SymbolExtractor.StructuredData.cs
  - src/CodeIndex/Indexer/DependencyPackageExtractor.cs
---

## English

- **Published trimmed builds now index JSON keys without reflection (#4709)** — JSON and dependency-lock extraction now encode property names without reflection-based serialization, so files containing keys such as `$schema` or `node_modules/...` no longer leave rebuilt indexes partial.

## 日本語

- **公開済み trimmed build が reflection を使わず JSON key を index するようになりました (#4709)** — JSON および dependency-lock 抽出は property name を reflection-based serialization なしで encode するため、`$schema` や `node_modules/...` などの key を含むファイルでも rebuild 後の index が partial にならなくなりました。
