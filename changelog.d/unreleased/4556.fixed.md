---
category: fixed
issues:
  - 4556
affected:
  - src/CodeIndex/Database/DbContext.cs
---

## English

- **SQLite `CANTOPEN` validation failures now report their actual cause (#4556)** — File and directory preflight, extended SQLite codes, and bounded OS probes distinguish missing databases, permission/traversal denials, WAL/SHM sidecar failures, invalid URIs, and unknown open failures while keeping provider messages and sensitive paths out of diagnostics.

## 日本語

- **SQLite `CANTOPEN` validation failure が実際の原因を報告するようになりました (#4556)** — file / directory preflight、SQLite extended code、bounded な OS probe により、DB missing、permission / traversal denial、WAL / SHM sidecar failure、不正 URI、不明 open failure を区別しつつ、provider message と機微な path を diagnostics へ出しません。
