---
category: fixed
issues:
  - 4574
affected:
  - src/CodeIndex/Cli/QueryCommandRunner.ArgParsing.cs
  - src/CodeIndex/Cli/QueryCommandRunner.Search.cs
  - src/CodeIndex/Cli/QueryCommandRunner.SearchRecipes.cs
  - src/CodeIndex/Cli/ProgramRunner.cs
  - src/CodeIndex/Cli/ProgramRunner.Dispatch.cs
  - src/CodeIndex/Cli/ConsoleUi.cs
  - tests/CodeIndex.Tests/ProgramRunnerTests.cs
  - USER_GUIDE.md
---

## English

- **Invalid CLI arguments now produce one command-specific diagnostic (#4574)** — derived inspect line ranges no longer look like duplicate explicit options, invalid field/check tokens suppress dependent empty-list errors, every `recipes` validation and byte-limit failure retains its own Usage, completion errors render once, and trailing formatting flags stay out of line-mode inspect queries.

## 日本語

- **不正な CLI 引数がコマンド固有の診断を 1 件だけ返すようになりました (#4574)** — inspect の派生行範囲を明示オプションの重複として扱わず、不正な field/check token の後に empty-list error を重ねず、`recipes` のすべての validation と byte-limit failure で固有の Usage を維持し、completion error を 1 回だけ描画し、line-mode inspect の query に末尾の format flag を混入させません。
